import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  
  button: {
    alignSelf:'center',
    position:'absolute',
    bottom:20,
    marginTop: 28,
    width:'80%'
  },

  main: 
  {flex:1, marginBottom: 80}
});
